import { styled } from "styled-components";

export const Container = styled.div`
  .noExpensesFound {
    list-style: none;
    padding: 0;

    .expenses-list__fallback {
      color: white;
      text-align: center;
    }
  }
`;
